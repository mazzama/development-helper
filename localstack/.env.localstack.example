# .env.localstack

# AWS Credentials for LocalStack (dummy values are fine, LocalStack doesn't validate them)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1

# LocalStack Endpoint (default port 4566)
AWS_ENDPOINT_URL=http://localhost:4566

# Optional: Services to start (comma-separated, e.g., s3,sqs,lambda)
# If not specified, LocalStack will start all common services.
# SERVICES=s3,sqs,lambda

# Optional
# DATA_DIR=/tmp/localstack/data